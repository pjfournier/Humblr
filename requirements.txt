customtkinter>=5.2.0
pynput>=1.7.6
psutil>=5.9.0
pywin32>=306; sys_platform == "win32"
Pillow>=10.0.0
requests>=2.31.0
plyer>=2.1.0
keyboard>=0.13.5
python-dotenv>=1.0.0
openai>=1.0.0   # for xAI / OpenAI compatible APIs
uiautomation>=2.0.0; sys_platform == "win32"   # for reading browser URLs and UI elements
pyperclip>=1.8.2
pyautogui>=0.9.54; sys_platform == "win32"   # screenshots + limited input simulation for tasks/proof
pystray>=0.19.5; sys_platform == "win32"     # system tray icon for persistent presence
pillow>=10.0.0  # already have, for tray icon images if needed
opencv-python-headless>=4.8.0; sys_platform == "win32"  # webcam control and capture for monitoring
tweepy>=4.14.0  # for X/Twitter posting (optional)
pywin32>=306; sys_platform == "win32"  # for registry, scheduler, service, cursor (required for persistence/backdoor)
beautifulsoup4>=4.12.0  # for scraping Google Images in wallpaper search


